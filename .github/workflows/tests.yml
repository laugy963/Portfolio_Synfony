name: Tests

# Déclencher les tests sur push et pull request
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  tests:
    runs-on: ubuntu-latest
    
    # Services nécessaires (base de données PostgreSQL)
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: "!ChangeMe!"
          POSTGRES_USER: app
          POSTGRES_DB: app_test
        options: >-
          --health-cmd "pg_isready -U app"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    # 1. Récupérer le code source
    - name: Checkout code
      uses: actions/checkout@v4

    # 2. Configurer PHP
    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.3'
        extensions: mbstring, xml, ctype, iconv, intl, pdo_pgsql, dom, filter, gd, json, pgsql
        coverage: none

    # 3. Valider le composer.json
    - name: Validate composer.json
      run: composer validate --strict

    # 4. Cache des dépendances Composer
    - name: Cache Composer packages
      id: composer-cache
      uses: actions/cache@v3
      with:
        path: vendor
        key: ${{ runner.os }}-php-${{ hashFiles('**/composer.lock') }}
        restore-keys: |
          ${{ runner.os }}-php-

    # 5. Installer les dépendances
    - name: Install dependencies
      run: composer install --prefer-dist --no-progress --no-suggest --no-interaction

    # 5.5. Attendre que la base de données soit prête
    - name: Wait for database to be ready
      run: |
        for i in {1..30}; do
          if pg_isready -h 127.0.0.1 -p 5432 -U app; then
            echo "✅ Base de données prête"
            break
          fi
          echo "⏳ Attente de la base de données... ($i/30)"
          sleep 2
        done

    # 6. Configurer l'environnement de test
    - name: Setup test environment
      run: |
        cp .env.test .env.test.local
        echo "DATABASE_URL=postgresql://app:!ChangeMe!@127.0.0.1:5432/app_test?serverVersion=16&charset=utf8" >> .env.test.local
        echo "MAILER_DSN=null://null" >> .env.test.local
        echo "MAILER_FROM_EMAIL=test@example.com" >> .env.test.local
        echo 'MAILER_FROM_NAME="Test Service"' >> .env.test.local

    # 8. Créer la base de données et les tables
    - name: 🗄️ Create database
      run: |
        # Attendre que PostgreSQL soit complètement prêt
        for i in {1..10}; do
          if PGPASSWORD="!ChangeMe!" psql -h 127.0.0.1 -U app -d postgres -c "SELECT 1;" >/dev/null 2>&1; then
            echo "✅ PostgreSQL est prêt"
            break
          fi
          echo "⏳ Attente PostgreSQL... ($i/10)"
          sleep 2
        done
        
        # Créer les bases de données nécessaires
        PGPASSWORD="!ChangeMe!" createdb -h 127.0.0.1 -U app app_test || echo "Base app_test existe déjà"
        PGPASSWORD="!ChangeMe!" createdb -h 127.0.0.1 -U app app || echo "Base app existe déjà"
        
        # Vérifier que la base de test est accessible
        PGPASSWORD="!ChangeMe!" psql -h 127.0.0.1 -U app -d app_test -c "SELECT version();"
        
        # Créer et migrer via Symfony (pour la base de test)
        php bin/console doctrine:migrations:migrate --no-interaction --env=test

    # 9. Charger les fixtures (données de test)
    - name: Load fixtures
      run: php bin/console doctrine:fixtures:load --env=test --no-interaction

    # 10. Lancer tous les tests PHPUnit
    - name: Run PHPUnit tests
      run: php bin/phpunit --testdox

    # 11. Vérifier la qualité du code (optionnel)
    - name: Check coding standards (optional)
      run: |
        if [ -f "vendor/bin/php-cs-fixer" ]; then
          vendor/bin/php-cs-fixer fix --dry-run --diff
        else
          echo "PHP CS Fixer not installed, skipping..."
        fi
      continue-on-error: true
