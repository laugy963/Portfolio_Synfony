<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Réinitialisation de votre mot de passe</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f9fafb;
            margin: 0;
            padding: 0;
        }
        .email-container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .header {
            background: linear-gradient(135deg, #2563eb 0%, #14b8a6 100%);
            color: white;
            padding: 30px;
            text-align: center;
        }
        .header h1 {
            margin: 0;
            font-size: 24px;
            font-weight: 700;
        }
        .content {
            padding: 30px;
            line-height: 1.6;
            color: #1f2937;
        }
        .button {
            display: inline-block;
            background: linear-gradient(135deg, #2563eb 0%, #14b8a6 100%);
            color: #000000 !important;
            text-decoration: none;
            padding: 15px 30px;
            border-radius: 25px;
            font-weight: 700;
            margin: 20px 0;
            text-align: center;
            border: 3px solid #000000;
            font-size: 16px;
            letter-spacing: 0.5px;
        }
        .button:hover {
            background: #1e40af;
            color: #000000 !important;
        }
        /* Styles spécifiques pour Outlook */
        .outlook-button {
            background-color: #2563eb !important;
            border: 3px solid #000000 !important;
            color: #000000 !important;
            text-decoration: none !important;
        }
        .footer {
            background-color: #f3f4f6;
            padding: 20px;
            text-align: center;
            font-size: 14px;
            color: #6b7280;
        }
        .warning {
            background-color: #fef2f2;
            border-left: 4px solid #dc2626;
            padding: 15px;
            margin: 20px 0;
            border-radius: 0 8px 8px 0;
        }
    </style>
</head>
<body>
    <div class="email-container">
        <div class="header">
            <h1>🔐 Réinitialisation de mot de passe</h1>
        </div>
        
        <div class="content">
            <h2>Bonjour {{ user.firstname }} !</h2>
            
            <p>Vous avez demandé la réinitialisation de votre mot de passe pour votre compte Portfolio.</p>
            
            <p>Pour réinitialiser votre mot de passe, cliquez sur le bouton ci-dessous :</p>
            
            <div style="text-align: center; margin: 30px 0;">
                <a href="{{ url('app_reset_password', {token: resetToken.token}) }}" 
                   class="button" 
                   style="display: inline-block; background: linear-gradient(135deg, #2563eb 0%, #14b8a6 100%); color: #000000 !important; text-decoration: none; padding: 15px 30px; border-radius: 25px; font-weight: 700; text-align: center; border: 3px solid #000000; font-size: 16px; letter-spacing: 0.5px;">
                    🔑 RÉINITIALISER MON MOT DE PASSE
                </a>
            </div>
            
            <p>Si le bouton ne fonctionne pas, vous pouvez copier et coller le lien suivant dans votre navigateur :</p>
            <p style="word-break: break-all; background-color: #f3f4f6; padding: 10px; border-radius: 4px; font-family: monospace;">
                {{ url('app_reset_password', {token: resetToken.token}) }}
            </p>
            
            <div class="warning">
                <strong>⚠️ Important :</strong> Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
            </div>
            
            <p><strong>Si vous n'avez pas demandé cette réinitialisation :</strong></p>
            <ul>
                <li>Ignorez simplement cet email</li>
                <li>Votre mot de passe actuel reste inchangé</li>
                <li>Aucune action n'est requise de votre part</li>
            </ul>
        </div>
        
        <div class="footer">
            <p>&copy; {{ "now"|date("Y") }} Portfolio. Tous droits réservés.</p>
            <p>Cet email a été envoyé automatiquement, merci de ne pas y répondre.</p>
        </div>
    </div>
</body>
</html>
