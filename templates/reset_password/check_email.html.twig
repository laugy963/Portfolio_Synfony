{% extends 'base.html.twig' %}

{% block title %}Email envoyé{% endblock %}

{% block body %}
<div class="container page-container content-fade-in">
    <!-- En-tête avec sous-titre -->
    <header class="text-center mb-5">
        <h1 class="h2 mb-3">Email envoyé !</h1>
    </header>

    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="form-container text-center">
                <div class="mb-4">
                    <i class="fas fa-envelope-open-text" style="font-size: 4rem; color: var(--primary-blue); margin-bottom: 1rem;"></i>
                </div>
            
            <h2 class="form-title mb-3">Vérifiez votre boîte email</h2>
            
            <div class="alert alert-success mb-4">
                <i class="fas fa-check-circle me-2"></i>
                Si un compte correspondant à votre email existe, nous venons d'envoyer un email contenant un lien pour réinitialiser votre mot de passe.
            </div>
            
            <p class="mb-3">
                <strong>Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</strong>
            </p>
            
            <p class="mb-4">
                Si vous ne recevez pas d'email, vérifiez votre dossier spam ou 
                <a href="{{ path('app_forgot_password_request') }}" class="link-primary">réessayez</a>.
            </p>

            <div class="form-footer">
                <a href="{{ path('app_login') }}" class="btn btn-primary">
                    <i class="fas fa-arrow-left me-2"></i>Retour à la connexion
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
